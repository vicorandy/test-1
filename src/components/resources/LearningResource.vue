<template>
  <li v-for="resource in resources" :key="resource.id">
    <base-card>
      <header>
        <h3>{{ resource.title }}</h3>
        <base-button mode="flat" v-on:click="deleteResource(resource.id)"
          >Delete</base-button
        >
      </header>
      <div>
        <p>{{ resource.description }}</p>
        <nav>
          <a :href="resource.link">View Resource</a>
        </nav>
      </div>
    </base-card>
  </li>
</template>
<script>
export default {
  inject: ['deleteResource'],
  props: {
    resources: {
      type: Array,
    },
  },
};
</script>
<style scoped>
li {
  margin: 29px auto;
  max-width: 40rem;
}
header {
  display: flex;
  justify-content: space-between;
  align-items: center;
}
h3 {
  font-size: 1.25rem;
  margin: 0.5rem 0;
}
p {
  margin: 00.5rem 0;
}
a {
  text-decoration: none;
  color: #ce5c00;
}
a:hover,
a:active {
  color: #c89300;
}
</style>
